<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lu.mapper.SpeciesNetworkMapper">
    <select id="createCompoundsList" resultType="Compound">
        select *
        from compound
    </select>
    <select id="getCIdList" resultType="string">
        select c_id
        from compound
    </select>
</mapper>